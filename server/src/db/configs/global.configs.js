export default {
  sss_no: {}, // Left empty because it is auto increment
  sss_amount: {
    min: 200, // custom, not a database metadata
    max: 999999999.99 // custom, not a database metadata
  },

  ec_amount: {
    min: 200, // custom, not a database metadata
    max: 999999999.99 // custom, not a database metadata
  }
};
